{
  "enable_txt2img": {
    "description": "启用文生图功能",
    "type": "bool",
    "default": true,
    "hint": "开启后允许使用文生图功能"
  },
  "server_url": {
    "description": "ComfyUI 服务器地址",
    "type": "string",
    "default": "http://127.0.0.1:8188",
    "hint": "ComfyUI API 服务器的完整地址，例如 http://127.0.0.1:8188"
  },
  "timeout": {
    "description": "生成超时时间（秒）",
    "type": "int",
    "default": 300,
    "hint": "等待图片生成的最长时间，超时将返回失败"
  },
  "default_negative_prompt": {
    "description": "默认负面提示词",
    "type": "text",
    "default": "bad hands, low quality, blurry",
    "hint": "文生图时的默认负面提示词"
  },
  "default_chain": {
    "description": "默认使用合并转发",
    "type": "bool",
    "default": false,
    "hint": "是否默认以合并转发形式发送图片（仅 aiocqhttp 平台支持）"
  },
  "txt2img_workflow": {
    "description": "文生图工作流文件名",
    "type": "string",
    "default": "example_text2img.json",
    "hint": "文生图工作流文件名，文件需放在 plugin_data/astrbot_plugin_comfyui_hub/workflows/ 目录下，留空或文件不存在则使用默认example_text2img.json"
  },
  "txt2img_positive_node": {
    "description": "文生图正面提示词节点ID",
    "type": "string",
    "default": "6",
    "hint": "工作流中正面提示词的节点编号"
  },
  "txt2img_negative_node": {
    "description": "文生图负面提示词节点ID",
    "type": "string",
    "default": "7",
    "hint": "工作流中负面提示词的节点编号"
  },
  "resolution_node": {
    "description": "分辨率节点ID",
    "type": "string",
    "default": "",
    "hint": "控制分辨率的节点编号，留空则自动查找 EmptyLatentImage 节点"
  },
  "resolution_width_field": {
    "description": "宽度字段名",
    "type": "string",
    "default": "width",
    "hint": "分辨率节点中宽度参数的字段名"
  },
  "resolution_height_field": {
    "description": "高度字段名",
    "type": "string",
    "default": "height",
    "hint": "分辨率节点中高度参数的字段名"
  },
  "upscale_node": {
    "description": "超分节点ID",
    "type": "string",
    "default": "",
    "hint": "控制超分倍率的节点编号，留空则不控制"
  },
  "upscale_scale_field": {
    "description": "超分倍率字段名",
    "type": "string",
    "default": "resize_scale",
    "hint": "超分节点中倍率参数的字段名"
  },
  "use_astrbot_llm": {
    "description": "使用 LLM 审查",
    "type": "bool",
    "default": true,
    "hint": "开启后将使用 LLM 进行提示词审查"
  },
  "censorship_prompt": {
    "description": "审查系统提示词",
    "type": "string",
    "default": "你是一个内容安全审核员。你需要判断用户输入的图片生成提示词是否包含直接暴露隐私部位（包括乳头）或生殖器官的内容，或者生成的图片是否包含血腥、恶心、恐怖元素。\n\n规则：\n1. 只有直接暴露隐私部位（包括乳头）、生殖器官，或者生成的图片包含血腥、恶心、恐怖元素时才判定为违规\n2. 不包含上述内容的均判定为不违规\n3. 对于隐晦暗示（如 'pussy juice' 等词语）但不直接暴露的内容，判定为不违规\n\n请回答 yes 或 no（小写，只回答一个单词）：\n- 如果违规（包含直接暴露隐私部位、生殖器官、血腥、恶心、恐怖元素），回答 yes\n- 如果不违规，回答 no",
    "hint": "AI 审查时使用的系统提示词（system_prompt），用户输入的文本将作为 prompt 单独传递"
  },
  "llm_provider_id": {
    "description": "LLM 提供商",
    "type": "string",
    "hint": "选择用于审查的 LLM 提供商，留空则使用会话默认提供商",
    "_special": "select_provider",
    "default": ""
  },
  "admin_bypass_censorship": {
    "description": "管理员绕过审查",
    "type": "bool",
    "default": true,
    "hint": "开启后管理员发送的绘图请求将跳过审查系统"
  },
  "enable_tagger": {
    "description": "启用图片标签识别功能",
    "type": "bool",
    "default": true,
    "hint": "开启后允许使用图片标签识别功能"
  },
  "tagger_workflow": {
    "description": "Tagger工作流文件名",
    "type": "string",
    "default": "",
    "hint": "图片标签识别工作流文件名，文件需放在 plugin_data/astrbot_plugin_comfyui_hub/workflows/ 目录下，留空则使用默认example_tagger.json"
  },
  "tagger_input_node": {
    "description": "Tagger输入节点ID",
    "type": "string",
    "default": "",
    "hint": "Tagger工作流中LoadImage节点的编号，留空则自动查找"
  },
  "tagger_output_node": {
    "description": "Tagger输出节点ID",
    "type": "string",
    "default": "",
    "hint": "Tagger工作流中文本输出的节点编号"
  }
}
